source 'https://rubygems.org'

ruby '>= 3.2.0'

# ActiveGenie - The Lodash for GenAI
gem 'active_genie'

# Web server
gem 'sinatra'
gem 'puma'

# Logging
gem 'logger'

# Environment variables
gem 'dotenv'

# JSON parsing (built-in since Ruby 2.5, but kept for compatibility)
gem 'multi_json'

# Testing
group :development, :test do
  gem 'rack-test'
  gem 'rspec', '~> 3.12'
end